# Preprocessing Lab 1

## Step 1

1. download dataset `credit-g` from this url https://www.openml.org/search?type=data&sort=runs&status=active&id=31
2. install useful python package (scipy , pandas, scikit-learn) found in requirements.txt
3. load data with `scipy` and create dataframe with `pandas`

## Step 2 
1. Check duplicated if exist remove the first with `pandas`
2. Determine which features are continuous and which are categorical
3. Detect missing data 
4. Detect outlire

## Step 3
1. Imputate missing data with regression
2. Encoding the categorical features

